import React, { Children } from 'react'

const Card = ({Children}) => {
  return (
    <div>
      {/* {name} */}
      {/* {props.children} */}
      {Children}
    </div>
  )
}

export default Card
